```
/reservation-backend
├── cmd/                 # 后端程序入口，工具程序入口
│   ├── tools/           # 工具程序
│   └── api/             # 后端程序
├── bin/                 # 可执行程序
│   ├── main0              
│   ├── main1            
│   └── mian2            
├── deplot/              # 部署初始化文件
│   ├── Mysql/
│   └── nginx/
├── internal/            # 核心内部代码
│   ├── auth/            # 领域1：认证与鉴权 (JWT, OpenID)
│   ├── reservation/     # 领域2：核心预约逻辑 (CRUD, 审核, 锁定)
│   ├── notification/    # 领域3：消息通知 (微信模板消息, 短信)
│   ├── platform/        # 公共组件：数据库连接, Redis 连接, 日志
│   └── pkg/             # 第三方 SDK 封装 (微信 SDK, 门锁 SDK)
├── configs/             # 配置文件
├── doc/                 # 系统开发，学习过程记录的知识点，笔记
├── docs/                # swagger 生成的 api 文件
├── script/              # 测试、部署脚本
├── skills/              # ai agent skills 封装，避免重复写提示词
└── go.mod
```