```
/reservation-backend
├── cmd/
│   └── api/             # 统一入口 (未来可拆为多个 cmd)
├── internal/
│   ├── auth/            # 领域1：认证与鉴权 (JWT, OpenID)
│   ├── reservation/     # 领域2：核心预约逻辑 (CRUD, 审核, 锁定)
│   ├── notification/    # 领域3：消息通知 (微信模板消息, 短信)
│   ├── platform/        # 公共组件：数据库连接, Redis 连接, 日志
│   └── pkg/             # 第三方 SDK 封装 (微信 SDK, 门锁 SDK)
├── configs/             # 配置文件
├── api/                 # 如果你使用 Swagger/Protobuf，放这里
└── go.mod
```